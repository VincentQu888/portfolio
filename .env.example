# Blog view counter (Upstash Redis). Without these, the counter simply hides.
# Get them from an Upstash Redis database (or Vercel → Storage → Upstash),
# then add the same two vars in your Vercel project's Environment Variables.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
